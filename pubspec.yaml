name: college_portal
description: Academic Incharge and Student Portal App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  firebase_auth: ^5.0.0
  intl: ^0.19.0
  file_picker: ^8.0.0
  url_launcher: ^6.2.5
  flutter_pdfview: ^1.3.2
  http: ^1.2.0

flutter:
  uses-material-design: true
